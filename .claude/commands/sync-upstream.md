Sync this fork with the upstream Classiq repository:
1. Fetch from upstream (https://github.com/Classiq/classiq-library.git)
2. Merge upstream/main into local main branch
3. Push the updated main branch to origin (the fork)
4. Show a summary of how many commits were synced

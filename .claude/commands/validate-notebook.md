Validate a Jupyter notebook in this repository:
1. Check if the notebook has a corresponding .metadata.json file
2. Validate the metadata has required fields (title, description, tags)
3. Check if the notebook follows the repository structure
4. Verify imports and basic syntax
5. Report any issues found

Ask the user which notebook to validate if not specified.
